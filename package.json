{
  "name": "gather2gether-edge",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test:edge": "node --test edge_test/*.test.mjs",
    "check:edge": "wrangler pages functions build --outdir .wrangler/functions-build",
    "deploy:cloudflare": "wrangler pages deploy cloudflare/public --branch main"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260823.1",
    "wrangler": "4.125.0"
  }
}
